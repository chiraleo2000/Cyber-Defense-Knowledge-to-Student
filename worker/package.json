{
  "name": "check-kon-oon-agent-proxy",
  "private": true,
  "description": "Cloudflare Worker proxy สำหรับผู้ช่วย AI (Pathumma)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
