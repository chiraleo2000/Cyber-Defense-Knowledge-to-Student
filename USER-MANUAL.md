# คู่มือการใช้งาน "เช็คก่อนโอน" + ผลการทดสอบระบบ

เอกสารนี้เป็นคู่มือการใช้งานเว็บไซต์/เว็บแอป และสรุป **ผลการทดสอบจริง** ที่ผ่านการตรวจสอบแล้ว
ก่อนเผยแพร่ (ทดสอบเมื่อ 3 กันยายน 2569)

🌐 **เว็บไซต์:** https://chiraleo2000.github.io/Cyber-Defense-Knowledge-to-Student/

---

## 1. ส่วนประกอบของระบบ

| ส่วน | ที่อยู่ | ใช้ทำอะไร |
|---|---|---|
| เว็บไซต์ข้อมูล | หน้าแรกของ Pages | สถิติ คดีจริง คู่มือ คำแนะนำ ดาวน์โหลดโปสเตอร์/PDF |
| เว็บแอปประเมินความเสี่ยง | `/webapp/` | ตรวจวิดีโอคอล สแกนข้อความ ตรวจภาพ ถอดเสียง ผู้ช่วย AI |
| Agent (ผู้ช่วย AI) | ในเว็บแอป แท็บ "ผู้ช่วย AI" | ตอบคำถามด้วยโมเดลไทย Pathumma (ต้อง deploy worker ก่อน) |

---

## 2. วิธีใช้งานเว็บไซต์ข้อมูล

1. เปิดหน้าเว็บหลัก จะเห็นเมนูหัวข้อทางซ้าย (บนมือถือกดปุ่ม ☰)
2. คลิกหัวข้อเพื่ออ่านเนื้อหา — ระบบเป็น Single Page App เปลี่ยนหน้าโดยไม่โหลดใหม่
3. ช่องค้นหาด้านบนซ้ายใช้กรองหัวข้อในเมนูได้
4. ปุ่ม ◐ มุมขวาบนสลับธีมสว่าง/มืด/อัตโนมัติ
5. หน้า **ดาวน์โหลด** มีทั้งไฟล์ PDF (ทุกหน้า) และโปสเตอร์ 5 ภาพ พร้อมคำโพสต์ Facebook/Instagram

### หน้าเนื้อหาทั้งหมด (ชื่อไฟล์เป็นภาษาไทย)

| หน้า | ไฟล์ Markdown | ไฟล์ PDF |
|---|---|---|
| หน้าแรก | `หน้าแรก.md` | `หน้าแรก.pdf` |
| ถูกหลอกแล้วทำอย่างไร + หลัก 4 ไม่ | `ถูกหลอกแล้วทำอย่างไร.md` | `ถูกหลอกแล้วทำอย่างไร.pdf` |
| สถิติและคดีจริงในไทย | `คดีจริงในไทย.md` | `คดีจริงในไทย.pdf` |
| มุมมองระดับโลก | `มุมมองระดับโลก.md` | `มุมมองระดับโลก.pdf` |
| การวิเคราะห์สถานการณ์ | `การวิเคราะห์สถานการณ์.md` | `การวิเคราะห์สถานการณ์.pdf` |
| คู่มือรับมือ | `คู่มือรับมือ.md` | `คู่มือรับมือ.pdf` |
| ข้อเสนอแนะเชิงนโยบาย | `ข้อเสนอแนะเชิงนโยบาย.md` | `ข้อเสนอแนะเชิงนโยบาย.pdf` |
| แหล่งที่มาของสื่อ | `แหล่งที่มาของสื่อ.md` | `แหล่งที่มาของสื่อ.pdf` |
| แบบสำรวจความตระหนักรู้ | `แบบสำรวจความตระหนักรู้.md` | `แบบสำรวจความตระหนักรู้.pdf` |
| ดาวน์โหลด | `ดาวน์โหลด.md` | `ดาวน์โหลด.pdf` |

---

## 3. วิธีใช้งานเว็บแอปประเมินความเสี่ยง (`/webapp/`)

เว็บแอปทำงานในเบราว์เซอร์ล้วน ๆ ไม่ส่งข้อมูลออกนอกเครื่อง (ยกเว้นผู้ช่วย AI ที่ส่งข้อความไปประมวลผล)

- **ตรวจสอบวิดีโอคอล** — ตอบคำถามสังเกตสัญญาณวิดีโอคอลปลอม ระบบให้คะแนนความเสี่ยง
- **สแกนข้อความ** — วางบทสนทนาที่ได้รับ ระบบไฮไลต์คำ/วลีเสี่ยง
- **ตรวจสอบภาพ/เอกสาร** — อัปโหลดภาพเพื่อดู EXIF และเกณฑ์เอกสารปลอม
- **ถอดเสียงสนทนา** — แปลงไฟล์เสียงเป็นข้อความด้วย Whisper (โอเพนซอร์ส รันในเบราว์เซอร์)
- **ผู้ช่วย AI** — แชทถามสถานการณ์ที่สงสัย (ดูวิธีเปิดใช้งานเต็มรูปแบบใน [`AGENT-SETUP.md`](AGENT-SETUP.md))

---

## 4. ผลการทดสอบระบบ (ทดสอบจริง 3 ก.ย. 2569)

ทดสอบโดยเสิร์ฟโฟลเดอร์ `docs/` ด้วยเว็บเซิร์ฟเวอร์ในเครื่อง แล้วเรียกทุก URL เพื่อยืนยันว่าโหลดได้จริง
(ตรวจ HTTP status, ขนาดไฟล์, ชนิดไฟล์ และลิงก์ภายในทุกหน้า)

### สรุปผล: ผ่าน 30 / เตือน 1 (ไม่ใช่ข้อผิดพลาด)

**ไฟล์หลักของระบบ — ผ่านทั้งหมด**

| รายการ | ผล | HTTP | ขนาด |
|---|---|---|---|
| `index.html` | ✅ ผ่าน | 200 | 2,550 bytes |
| `assets/js/app.js` | ✅ ผ่าน | 200 | 10,397 bytes |
| `assets/js/marked.min.js` | ✅ ผ่าน | 200 | 35,479 bytes |
| `assets/css/style.css` | ✅ ผ่าน | 200 | 9,079 bytes |
| `webapp/index.html` | ✅ ผ่าน | 200 | 124,537 bytes |
| `.nojekyll` | ⚠️ ไฟล์ว่าง (ถูกต้องตามดีไซน์) | 200 | 0 bytes |

**หน้าเนื้อหาทั้ง 10 หน้า (route .md) — ผ่านทั้งหมด (HTTP 200 มีเนื้อหาครบ)**
หน้าแรก, ถูกหลอกแล้วทำอย่างไร, คดีจริงในไทย, มุมมองระดับโลก, การวิเคราะห์สถานการณ์,
คู่มือรับมือ, ข้อเสนอแนะเชิงนโยบาย, แหล่งที่มาของสื่อ, แบบสำรวจความตระหนักรู้, ดาวน์โหลด

**ไฟล์ PDF ทั้ง 10 ไฟล์ — ผ่านทั้งหมด** (HTTP 200, Content-Type `application/pdf`, ขนาด 41KB–156KB)

**โปสเตอร์ 5 ภาพ — ผ่านทั้งหมด** (HTTP 200, PNG, ขนาด ~234KB–250KB ต่อภาพ)

**ลิงก์ภายในระหว่างหน้า — ไม่พบลิงก์เสียแม้แต่จุดเดียว** (ตรวจทุกลิงก์ `.md`/ไฟล์ในทุกหน้า)

**โครงสร้างโค้ด** — `app.js` และ `worker/src/index.js` วงเล็บสมดุลครบ ไม่พบข้อผิดพลาดไวยากรณ์

> **หมายเหตุเรื่อง `.nojekyll`:** ที่ขึ้น "เตือน" เพราะเป็นไฟล์ขนาด 0 ไบต์ ซึ่ง **ถูกต้องแล้ว** —
> ไฟล์นี้ต้องว่างโดยตั้งใจ (ใช้บอก GitHub Pages ให้ข้ามการประมวลผล Jekyll) ไม่ใช่ข้อผิดพลาด

### Agent (Pathumma) — ทดสอบการเชื่อมต่อ API แล้ว

- ทดสอบเรียก ThaiLLM API (`http://thaillm.or.th/api/v1/chat/completions`) โมเดล
  `pathumma-thaillm-qwen3-8b-think-3.0.0` ได้ผลตอบกลับ **HTTP 200** สำเร็จ
- ยืนยันว่าโมเดลใส่ร่องรอยการคิดใน `<think>...</think>` จริง และ worker ตัดออกให้อัตโนมัติถูกต้อง
- พบและแก้แล้ว: endpoint อยู่หลัง Cloudflare จึงบล็อก request ที่ไม่มี User-Agent (error 1010) —
  worker เพิ่มการส่ง User-Agent แล้ว ทดสอบซ้ำได้ HTTP 200

---

## 5. ข้อจำกัดที่ทราบ (ไม่ใช่ข้อผิดพลาด)

- **เว็บแอปโหลดฟอนต์จาก Google Fonts CDN** และไลบรารี Whisper จาก jsDelivr CDN — หาก CDN ถูกบล็อก
  ตัวอักษรจะใช้ฟอนต์ระบบแทน (มี fallback `system-ui` ทุกจุด ภาษาไทยยังอ่านได้) และฟีเจอร์ถอดเสียง
  จะใช้ไม่ได้ ส่วนเว็บไซต์ข้อมูลหลัก self-host ฟอนต์ Sarabun ครบแล้ว
- **ผู้ช่วย AI** ต้อง deploy Cloudflare Worker ก่อนจึงจะตอบด้วย Pathumma จริง ถ้ายังไม่ deploy จะตอบ
  แบบกฎเบื้องต้น (ออฟไลน์) แทน
- **ความแม่นยำการถอดเสียงภาษาไทย** ของ Whisper รุ่นเล็กในเบราว์เซอร์อาจต่ำกว่าบริการเฉพาะทาง

---

## 6. การดูแล/อัปเดตเนื้อหา

1. แก้ไฟล์ `.md` ชื่อไทยในโฟลเดอร์ `docs/content/`
2. รัน `python tools/build_pdfs.py` เพื่อสร้าง PDF ให้ตรงกับเนื้อหาใหม่
3. รันชุดทดสอบซ้ำ: เสิร์ฟ `docs/` แล้วรัน `python tools/_sitetest.py` (ควรได้ PASS ครบ ยกเว้น `.nojekyll` ที่ว่างโดยตั้งใจ)
4. commit แล้ว push เข้า `main` — GitHub Actions จะ deploy ขึ้น Pages อัตโนมัติ

*เอกสารทดสอบนี้จัดทำตามหลักความโปร่งใสของโครงการ — ผลทดสอบทั้งหมดมาจากการเรียก URL จริงในเครื่อง
ไม่ใช่การคาดเดา*
